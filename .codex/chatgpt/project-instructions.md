# Large File Upload — ChatGPT Project Instructions

Project Instructions Version: 1
Exact repository: `pengcc/large-file-upload`

For project work:

1. use the connected GitHub repository and fetch current `main:AGENTS.md` before relying on repository-local routing;
2. fetch current `main:.codex/chatgpt/project-instructions.md` and compare its `Project Instructions Version` with this configured Project version;
3. if the versions differ, report `Project refresh required` and do not pretend the configured bootstrap is current;
4. follow current `AGENTS.md` routing and load only the repository-owned Project Memory source, collaboration contract or principle, rule, skill, or exact target that the current task actually needs;
5. use current `pengcc/large-file-upload` repository truth and exact targets for repository-aware claims and actions;
6. do not depend on `pengcc/badminton-club-app` or another product repository for current runtime, product, architecture, deployment, Project Memory, or workflow truth; externally adopted semantics are source/provenance evidence only after they have been materialized into this repository;
7. do not require static configured-Project Resource Files as semantic authorities when the current repository already owns the needed source;
8. if a requested workflow or capability is absent from the current repository, use its current fallback or STOP boundary rather than inventing an equivalent capability; and
9. preserve the external-action and publication boundaries defined by current repository owners and the user's current request.

The configured ChatGPT Project is a runtime projection of this small bootloader. This repository owns current routing, project truth, adopted collaboration semantics, and workflow mechanics.
